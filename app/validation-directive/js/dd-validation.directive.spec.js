describe('simple-directive', function () {
    beforeEach(function () {
        module('simple-directive');
    });

    it('cds', inject(function () {
        // given
        expect(true).toBeTruthy();
    }));
});